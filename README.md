First install

```
#To be completed
```

Update instructions

from setup to local :

```
# To be completed
```

from local to setup :

```
for fn in aliases gitconfig zshrc screenrc; do rsync -avcizR --stats ~/.$fn ~/setup/$fn; done
```
